`python:alpine` based [Docker Hub container](https://hub.docker.com/_/python/) 
with matplotlib, numpy, qiskit and Python3.

Building matplotlib takes a lot of time, so this comes with that prebuilt.
